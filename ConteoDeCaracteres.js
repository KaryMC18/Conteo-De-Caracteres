function contador(cadena) {
    const numCaracteres = cadena.length;

    console.log(`La cadena "${cadena}" tiene ${numCaracteres} caracteres.`);
}

  const cadena = "Hola, mundo!";
  contador(cadena);
  